{
  "name": "winners-history",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/winners-history raffle/winners-history",
    "create-local": "graph create --node http://localhost:8020/ raffle/winners-history",
    "remove-local": "graph remove --node http://localhost:8020/ raffle/winners-history",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 raffle/winners-history"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.30.1",
    "@graphprotocol/graph-ts": "0.27.0"
  }
}
